# Lokalizacja: /cloudbuild-orderflow.yaml

steps:
  # 1. Build image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build Image'
    args:
      - 'build'
      - '-t'
      - 'europe-central2-docker.pkg.dev/$PROJECT_ID/trading-bot-image/orderflow-engine:$SHORT_SHA'
      - '--file'
      - 'orderflow_engine/Dockerfile'
      - '.'

  # 2. Push image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push Image'
    args:
      - 'push'
      - 'europe-central2-docker.pkg.dev/$PROJECT_ID/trading-bot-image/orderflow-engine:$SHORT_SHA'

  # 3. Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy to Cloud Run'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'orderflow-engine'
      - '--image'
      - 'europe-central2-docker.pkg.dev/$PROJECT_ID/trading-bot-image/orderflow-engine:$SHORT_SHA'
      - '--region'
      - 'europe-central2'
      - '--platform'
      - 'managed'
      - '--service-account'
      - 'trading-bot-runner@$PROJECT_ID.iam.gserviceaccount.com'
      - '--min-instances=1'
      - '--max-instances=1'
      - '--no-cpu-throttling'
      - '--update-secrets'
      - 'BYBIT_API_KEY=bybit-api-key:latest,BYBIT_API_SECRET=bybit-api-secret:latest'
      - '--update-env-vars'
      - 'LIQ_CASCADE_THRESHOLD_USD=0,GCP_PROJECT=$PROJECT_ID,WS_OPEN_TIMEOUT_SEC=90,BOT_SERVICE_URL=https://trading-bot-service-785819958951.europe-central2.run.app/process-alerts,BOT_SERVICE_TIMEOUT_SEC=120,BOT_SERVICE_RETRIES=3,BOT_SERVICE_CONNECT_TIMEOUT_SEC=20'
      - '--clear-network'

images:
  - 'europe-central2-docker.pkg.dev/$PROJECT_ID/trading-bot-image/orderflow-engine:$SHORT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY